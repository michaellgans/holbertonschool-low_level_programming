#include <stdio.h>
#include <string.h>

/**
 * main - entry point
 *
 * Return: Always 0.
 */
int main(void)
{
	char str1[55];

	strcpy(str1, "\"Programming is like building a multilingual puzzle\n");
	puts(str1);
	return (0);
}
