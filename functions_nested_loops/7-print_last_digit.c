#include "main.h"

/**
 * print_last_digit - CHECK YOUR PROTOTYPE
 * @x: CHECK YOUR VAR
 * Return: CHECK YOUR PROTOTYPE
 */
int print_last_digit(int x)
{
	x = 0;

	while (x < 2)
	{
		x = (x % 10);
		return (x);
		x++;
	}
	return (0);
}
