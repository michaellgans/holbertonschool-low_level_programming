#include "main.h"
#include <unistd.h>
/**
 * _islower - DONE
 *
 * @c: DONE
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	if (_islower(c))
	{
		putchar(1);
	}
	else
	{
		putchar(0);
	}

	return (0);
}
