#include "main.h"
#include <stdio.h>

/**
 * _isupper - entry point
 * @c: variable
 * Return: int
 */

int _isupper(int c)

{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
return (9);
}
